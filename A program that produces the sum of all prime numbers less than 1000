#include <stdio.h> 
  
int main() 
{  
	int n,b,i,m; 
	 
	printf("1000 \n"); 
	scanf("%d",&m); 
	for(b=2;b<=m;b++) 
	{ 
	 
 	 i=2; 
 	while(b%i!=0) 
  	{ 
  		i++; 
  	} 
  	 
 	if(i==b) 
  	{ 
		printf("%d\n",b) ; 
		} 
		 
  } 
 	return 0; 
} 
